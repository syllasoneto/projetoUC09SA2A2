function multiplicaAxB(a, b) {
    var result =  b*a;
    
    // if(result % Math.round(result) > 0) {
    //     result = result.toFixed(6);
    // } else {
    //     result = Math.round(result);
    // }

    return result;
}